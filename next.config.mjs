/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["cdn.kastatic.org", "yt3.ggpht.com"], // Add the hostnames here
    },
  };
  
  export default nextConfig;
  